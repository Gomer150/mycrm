<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Список сделок</title>
</head>
<body>
    <h1>Список сделок</h1>

    <p>Вы вошли как: {{ request.user.username }} | <a href="{% url 'logout' %}">Выйти</a></p>

    {% if deals %}
        <table border="1" cellpadding="5" cellspacing="0">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Название сделки</th>
                    <th>Статус</th>
                    <th>Владелец</th>
                    <th>Детали</th>
                </tr>
            </thead>
            <tbody>
                {% for deal in deals %}
                <tr>
                    <td>{{ deal.id }}</td>
                    <td>{{ deal.name }}</td>
                    <td>{{ deal.stage.name }}</td>
                    <td>{{ deal.owner.username }}</td>
                    <td><a href="{% url 'deal_detail' deal.pk %}">Открыть</a></td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    {% else %}
        <p>Сделок пока нет.</p>
    {% endif %}
</body>
</html>
